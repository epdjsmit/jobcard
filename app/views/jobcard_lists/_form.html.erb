<%= form_for(@jobcard_list) do |f| %>
  <% if @jobcard_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobcard_list.errors.count, "error") %> prohibited this jobcard_list from being saved:</h2>

      <ul>
      <% @jobcard_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :Description %><br>
    <%= f.text_area :Description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
